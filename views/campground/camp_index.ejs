<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lucknow Locations</title>
</head> -->
<%layout('layout/boilerplate.ejs') %>
<!-- Map Section -->
<div id="map" class="w-[90%] h-[500px] mx-auto mt-8 rounded-lg shadow-md border border-gray-300"></div>

<!-- <body> -->
    <h2 class="text-4xl font-bold text-gray-800">These are the Prime camping locations in Lucknow</h2>
    <div class="mt-7">
        <a href="/campgrounds/new" class=" py-2 px-4 bg-blue-500 text-white font-semibold rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-300">Add New Camp</a>
    </div>
    <% for(let eachcamp of campgrounds){ %>
        <ul class="flex flex-col items-center space-y-6 p-4">
            <li class="flex flex-row bg-white shadow-lg rounded-lg border border-gray-200 w-[600px] p-4">
              
              <!-- Left Section (Image & Title) -->
              <div class="w-[250px] flex flex-col items-center">
                <div class="w-[220px] h-[150px] rounded-lg overflow-hidden shadow-md">
                  <img src="<%= eachcamp.image[0].url %>" alt="<%= eachcamp.title %>" class="w-full h-full object-cover">
                </div>
                <h2 class="text-lg font-semibold text-gray-800 mt-2"><%= eachcamp.title %></h2>
              </div>
          
              <!-- Right Section (Description & Link) -->
              <div class="flex-1 flex flex-col justify-between ml-6">
                <p class="text-gray-600 text-sm"><%= eachcamp.description %></p>
                <a href="/campgrounds/<%= eachcamp._id %>" class="text-blue-500 hover:text-blue-700 font-semibold mt-4">
                  View Details →
                </a>
              </div>
          
            </li>
          </ul>
          
        <%}%>
<!-- </body>
</html> -->
<script>
  const maptilerApiKey = '<%- process.env.MAPTILER_API_KEY %>';
</script>
<script src="/javascript/clusterMap.js"></script>